spring:
  application:
    name: realworld

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/realworld?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: {{secrets.MYSQL_PASSWORD}}
  jpa:
    hibernate:
      ddl-auto: create
